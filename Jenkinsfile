
pipeline {
  agent { label 'linux' }
    stages {
      stage('checkout') {
            steps {
                sh "git clone https://github.com/Rameshkumarsc/hello-world-war"
            }
        }
    }
}
